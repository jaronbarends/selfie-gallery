{
  "name": "selfie-gallery",
  "version": "0.0.1",
  "description": "swipe selfies from device to online gallery",
  "dependencies": {
    "express": "^4.16.3",
    "socket.io": "^2.1.1"
  },
  "engines": {
    "node": "8.11.x"
  },
  "scripts": {
    "preinstall": "npm install --save socket.io --msvs_version=2013",
    "start": "node socket-server.js"
  }
}
